<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>
<!-- Saved on ÐÇÆÚËÄ, °ËÔÂ 09, 2012, 8:36 ÏÂÎç -->
<!-- MuClient version 4.73 -->

<!-- Plugin "reconnect2" generated by Plugin Wizard -->

<muclient>
<plugin
   name="reconnect2"
   author="Ò¶ÖªÇï"
   id="083f0cf014dbc8895a197d2a"
   language="Lua"
   purpose="Ò¶ÖªÇïforÊé½£×Ô¶¯ÖØÁ¬·À·¢´ô²å¼þ"
   date_written="2012-08-09 20:34:56"
   requires="4.73"
   version="0.8"
   >
<description trim="y">
<![CDATA[
Ò¶ÖªÇïforÊé½£×Ô¶¯ÖØÁ¬·À·¢´ô²å¼þ
]]>
</description>

</plugin>

<!--  Script  -->


<script>
<![CDATA[

local auto_connect=true
local is_afk=false
local afk_cmd="set afk;unset afk"
local afk_sec=600 --Ä¬ÈÏÖµ
--Ò¶ÖªÇï°æ ×Ô¶¯Á¬½Ó·À·¢´ô²å¼þ0.8°æ
local function _init()
    --world.ColourNote("salmon", "", "afk:set_command cmd  - ÉèÖÃ AFK ÃüÁî£¬cmd ÎªÈÎºÎÄãÏë·¢ËÍµÄ MUD ÃüÁî")
    world.ColourNote("salmon", "", "afk:set_time seconds - ÉèÖÃ AFK µÄÊ±¼ä£¬seconds Îª´óÓÚ 0 Ð¡ÓÚ 3600 µÄÊý×Ö£¨Ãë£©,·¢ËÍÒ»¸öset afkÃüÁî")
    world.ColourNote("salmon", "", "afk:enable           - ÆôÓÃ AFK ²å¼þ")
    world.ColourNote("salmon", "", "afk:disable          - ½ûÓÃ AFK ²å¼þ")
    world.ColourNote("salmon", "", "afk:show_state       - ÏÔÊ¾ AFK µ±Ç°µÄ×´Ì¬")
	--alias
	world.AddAlias("afk_settime", "^afk:set_time (\\\d*)$", "set_AFKTime(%1)", alias_flag.Enabled + alias_flag.RegularExpression, "")
    world.SetAliasOption ("afk_settime", "send_to", 12) --Ïò½Å±¾·¢ËÍ

    --world.AddAlias("afk_cmd", "^afk:set_command (.*)$", "set_AFKCmd('%1')", alias_flag.Enabled + alias_flag.RegularExpression , "")
    --world.SetAliasOption ("afk_cmd", "send_to", 12) --Ïò½Å±¾·¢ËÍ

	world.AddAlias("afk_enable", "afk:enable", "Enable_AFK()", alias_flag.Enabled, "")
    world.SetAliasOption ("afk_enable", "send_to", 12) --Ïò½Å±¾·¢ËÍ

	world.AddAlias("afk_disable", "afk:disable", "Disable_AFK()", alias_flag.Enabled, "")
    world.SetAliasOption ("afk_disable", "send_to", 12) --Ïò½Å±¾·¢ËÍ

	world.AddAlias("afk_status", "afk:show_state", "Status_AFK()", alias_flag.Enabled, "")
    world.SetAliasOption ("afk_status", "send_to", 12) --Ïò½Å±¾·¢ËÍ
end

function OnPluginClose()
end

function OnPluginConnect()
   auto_connect=true
   button()
   ColourNote("white", "red", "Á¬ÉÏÊé½£ÊÀ½ç£¡£¡")
   world.EnableTimer("reconnect_timer",false)
   world.DeleteTimer("reconnect_timer")
   Enable_AFK()
end

function OnPluginDisable()
   print("×Ô¶¯Á¬½Ó²å¼þ±»¹Ø±Õ")
end

local connect_count=0
function OnPluginDisconnect()
  if is_afk==true then
     Disable_AFK()
  end
  if auto_connect==true then
    connect_count=0
    ColourNote("white", "red", "¶ÏÏß£¬Æô¶¯×Ô¶¯Á¬½Ó5Ãë¿ªÆôÁ¬½Ó")

           local tl = world.GetTimerList ()
           if tl then
              for k, v in ipairs (tl) do
               Note ("ÔÝÍ£ËùÓÐ¼ÆÊ±Æ÷"..v)
	           world.SetTimerOption (v, "enabled", "n")
              end  -- for
           end -- if we have any timers
    world.AddTimer ("reconnect_timer", 0, 0, 5, "sj_Connect()", timer_flag.Enabled+timer_flag.ActiveWhenClosed, "")
    world.SetTimerOption ("reconnect_timer", "send_to", 12)
  else
    ColourNote("white", "red", "Àë¿ªÊé½£ÊÀ½ç£¡£¡")
  end
end

function OnPluginEnable()
  print("×Ô¶¯Á¬½Ó²å¼þ±»¿ªÆô")
end

function sj_Connect()
   connect_count=connect_count+1
   if connect_count>9999 then
       ColourNote("white", "red", "Àë¿ªÊé½£ÊÀ½ç£¡£¡")
       world.EnableTimer("reconnect_timer",false)
      return
   end
   local reset_sec=5*connect_count
   local reset_min=0

   world.ColourNote("white", "red", "¶ÏÏß£¬Æô¶¯×Ô¶¯Á¬½Ó"..reset_sec.."Ãë¿ªÆôÁ¬½Ó")

   local hour=math.floor(reset_sec/3600)
   reset_sec=reset_sec-hour*3600
   local mins=math.floor(reset_sec/60)
   reset_sec=reset_sec-mins*60
   world.SetTimerOption("reconnect_timer","hour",hour)
   world.SetTimerOption("reconnect_timer","minute",mins)
   world.SetTimerOption("reconnect_timer","second",reset_sec)
   for k, v in pairs (GetWorldIdList ()) do
      if v==world.GetWorldID() then
            print("¼¤»î:world"..k)
            world.DoCommand("world"..k)
            world.DoCommand("Connect")

             --world.DoCommand("ResetAllTimers")
            local tl = world.GetTimerList ()
           if tl then
              for k, v in ipairs (tl) do
               Note ("¼ÆÊ±Æ÷»Ö¸´"..v)
	           world.SetTimerOption (v, "enabled", "y")
              end  -- for
           end -- if we have any timers
           break
      end
   end


   --world.Connect()
end


function mousedown()
  return function (flags, hotspot_id)
    --print(flags)

	if flags==16 then
      if auto_connect==true then
        auto_connect=false
		world.EnableTimer("reconnect_timer",false)
      else
        auto_connect=true
		world.EnableTimer("reconnect_timer",true)
      end
      button()
	end
  end
end

function switch_auto_connect(flag)--call
   auto_connect=flag
   button()
end

function disconnect()
  ColourNote("red", "black", "·¢´ôÍË³öÊé½£²»ÔÙ×Ô¶¯Á¬½Ó£¡£¡")
  auto_connect=false
  button()
end


function OnPluginInstall()
   ColourNote("white", "red", "Ò¶ÖªÇï°æ_for_Êé½£_ÖØÁ¬²å¼þ·À·¢´ô²å¼þ0.8°æ")
   _init()
   win="auto_connect_btn"
   WindowCreate(win,0,0,30,30,miniwin.pos_bottom_right,0,0x909090)
   local _mousedown=mousedown()
   _G["at_mousedown"]=_mousedown
   WindowAddHotspot(win, "auto_connect_hotspot",
                    0,  0, 30, 30,   -- rectangle
                   "",   -- MouseOver
                   "",   -- CancelMouseOver
                   "at_mousedown",  -- MouseDown
                   "",   -- CancelMouseDown
                   "",   -- MouseUp
                   "¹Ø±Õ»ò¿ªÆô×Ô¶¯Á¬½Ó",  -- tooltip text
                   cursor or 1, -- cursor
                   0)  -- flags
   button()
   world.AddTriggerEx ("idle", "^(> |)¶Ô²»Æð£¬ÄúÒÑ¾­·¢´ô³¬¹ý 20 ·ÖÖÓÁË£¬ÇëÏÂ´ÎÔÙÀ´¡£$", "disconnect()", trigger_flag.RegularExpression + trigger_flag.Replace+trigger_flag.Enabled+trigger_flag.KeepEvaluating, custom_colour.NoChange, 0, "", "", 12, 40)

end

function OnPluginSend(sText)
    if string.lower(sText)=="quit" then
      auto_connect=false
	  button()
	  Disable_AFK()
	else
	  Reset_AFK()
	end
   --print(sText)
   return true
end


function Reset_AFK()
    world.ResetTimer("afk_timer")
end

function Status_AFK()
    if is_afk==true then
       world.ColourNote("salmon", "", "AFK¿ªÆô")
	else
	   world.ColourNote("salmon", "", "AFK¹Ø±Õ")
	end
	world.ColourNote("salmon","","¼ì²é¼ä¸ôÊ±¼ä:"..afk_sec)
	world.ColourNote("salmon","","·¢ËÍµÄÃüÁî:"..afk_cmd)
end

local function convert_seconds(seconds)
    local hours = math.floor(seconds / 3600)
    seconds = seconds - (hours * 3600)
    local minutes = math.floor(seconds / 60)
    seconds = seconds - (minutes * 60)
    return hours, minutes, seconds
end

function Enable_AFK()
   local hours,minutes,seconds=convert_seconds(afk_sec)
	world.AddTimer ("afk_timer", hours, minutes, seconds, afk_cmd, timer_flag.ActiveWhenClosed+timer_flag.Replace, "")
    world.SetTimerOption ("afk_timer", "send_to", 10)
	world.EnableTimer("afk_timer",true)
	Reset_AFK()
	is_afk=true
	world.ColourNote("salmon", "", "AFK¿ªÆô")
end

function Disable_AFK()
    Reset_AFK()
	world.EnableTimer("afk_timer",false)
	is_afk=false
	world.ColourNote("salmon", "", "AFK¹Ø±Õ")
end

function set_AFKTime(sec) --mins sec ×î´ó²»³¬¹ý60
   afk_sec=sec
   world.ColourNote("salmon","","¼ì²é¼ä¸ôÊ±¼ä:"..afk_sec)
end

function set_AFKCmd(cmd)
   afk_cmd=cmd
   world.ColourNote("salmon","","·¢ËÍµÄÃüÁî:"..afk_cmd)
end

function button()
  win="auto_connect_btn"
	  --text add
	  WindowFont (win, "f", "ËÎÌå", 9, true, false, false, false)
	  local switch_name=""
	  if auto_connect==true then
	     switch_name="¡Ì¿ª"
		 ColourNote("red", "black", "×Ô¶¯Á¬½Ó¿ª")
	  else
	     switch_name="¡Á¹Ø"
		 ColourNote("red", "black", "×Ô¶¯Á¬½Ó¹Ø")
	  end
	 WindowRectOp (win, miniwin.rect_fill, 0,0,30,30,0x000010)  -- raised, filled, softer, flat
     WindowCircleOp (win, miniwin.circle_round_rectangle, 0, 0, 30, 30, 0xc0c0c0, 0, 1,0, 0, 9, 9)
	  WindowText (win, "f", switch_name,2,8,30,30,ColourNameToRGB ("red"), false) -- not Unicode
     WindowShow (win,  true)  -- show it
end



]]>
</script>


<!--  Plugin help  -->

<aliases>
  <alias
   script="OnHelp"
   match="reconnect2:help"
   enabled="y"
  >
  </alias>
</aliases>

<script>
<![CDATA[
function OnHelp ()
  world.Note (world.GetPluginInfo (world.GetPluginID (), 3))
end
]]>
</script>

</muclient>
